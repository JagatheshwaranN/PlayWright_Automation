// Assuming you have a reference to the password input element
var passwordInput = document.getElementById("password");

// Setting the value to "secret"
if (passwordInput) {
  passwordInput.value = "secret";
}